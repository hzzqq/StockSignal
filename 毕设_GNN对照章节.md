# 因子图卷积网络（GNN）与 GRU/Transformer 对照实验

> 毕设素材 · 可作为「事件因子序列编码」章节的一节（开题清单 #2）。所有数字来自 P1 项目同口径 head-to-head 实验（n=1,583,338，walk-forward 2022–2026，与 #1 完全同口径）。

## 实验动机

#1 已验证「更强时序编码器（Transformer）」相对 GRU 有 modest 但一致的优势。本小节进一步引入**因子图结构偏置**：假设 38 个因子之间存在相关性拓扑，用图卷积网络（GCN）在因子维做消息传递，显式建模因子间的非线性交互，再接时序 GRU + 注意力池化 + 回归头，预测 horizon=10 日超额收益。动机是：因子并非相互独立，图结构可能比 GRU 的朴素因子投影捕捉到更丰富的因子间关系。

## 实验设置

- **数据集**：`dataset_h10`（A 股多标的因子序列，预测 horizon=10 交易日超额收益）。
- **验证方式**：滚动 walk-forward，测试年 2022–2026。
- **统一超参（公平对照）**：隐层 `hidden=64`、层数 `=2`、回看 `seq_len=20`、训练 `epochs=15`、步长 `stride=5`、参与标的上限 `500`（默认全 1491 只）。
- **图结构（因子相关性图）**：`build_factor_graph` 用**训练期因子截面相关矩阵**，`|r|≥0.3` 阈值裁剪 + 对称化 + 自环，对称归一化邻接 `A_hat = D^{-1/2} A D^{-1/2}`；每 walk-forward 年重算（因子结构近似时不变）。
- **模型 `FactorGraphGCN`**：每个因子节点为标量 → `Linear(1, gnn_hidden)` 映射为节点特征 → GCN 在因子维消息传递 → 因子节点均值池化 → 接 GRU（时序）+ 加性注意力池化 + 回归头。与 `GRUAttention` 同接口、同超参，对比公平：时序感知完全由 GRU 提供，GCN 只在因子维做空间聚合。
- **基线**：`GRUAttention`（#1 基线）、`TransformerSignal`（#1 对照）。

## 评价指标

- **方向命中率**：`sign(pred)` 与 `sign(真实超额收益)` 一致、剔除平盘（真实收益≈0）的样本占比。锚 **49.1%**（StockSignal 决策闭环论文 1722/3507，>0 多 / <0 空 / =0 不计）。
- **IC / ICIR**：滚动验证下预测与真实收益的秩相关系数均值（IC）及其信息比率（ICIR）。
- **IC 正占比**：各年度 IC>0 的比例。

## 实验结果（诚实口径）

| 模型 | 方向命中率 | 相对 49.1% 锚 Δ | IC | ICIR | IC 正占比 | 样本数 |
|---|---|---|---|---|---|---|
| FactorGraphGCN（GNN，本小节） | 0.4948 | +0.0038 | 0.0076 | 0.1047 | 0.568 | 1,583,338 |
| GRUAttention（基线） | 0.5097 | +0.0187 | 0.0374 | 0.3462 | 0.653 | 1,583,338 |
| TransformerSignal（#1 对照） | 0.5130 | +0.0220 | 0.0465 | 0.3796 | 0.653 | 1,583,338 |

## 结果分析（如实表述，不夸大）

1. **GNN 在三项指标上全部落后于两个基线**：方向命中率 0.4948（仅比 49.1% 锚高 0.38pp，且低于 GRU 的 0.5097 与 Transformer 的 0.5130）；IC=0.0076 仅为 GRU（0.0374）的约 **1/5**、Transformer（0.0465）的约 **1/6**；ICIR=0.1047 同样远低于二者（0.3462 / 0.3796）；IC 正占比 0.568 也低于基线的 0.653。
2. **因子相关图聚合未带来增益，反而削弱信号**。可能原因：① 训练期相关矩阵是**静态、对称的弱归纳偏置**，对时序预测帮助有限，且跨年重算引入分布偏移；② 因子间交互已被 GRU 的时序编码**隐含捕捉**，显式图聚合反而引入跨因子噪声；③ 图构建（固定阈值 0.3 + 对称归一化）可能平滑掉判别性信息，GCN 层数/聚合方式也未针对该数据调优。
3. **与 #1 形成对照，框定适用边界**：在本文数据集上，「更强时序编码器（Transformer）」有 modest 增益，但「因子图结构偏置（GNN）」反而有害。说明该数据集的因子交互更接近**可加 / 时序主导**，而非图拓扑主导。图神经网络要在量化因子编码中生效，需要**动态图 / 可学习图**或 **richer 节点特征**（而非单因子标量），本实验未覆盖。

## 本章小结

在本文所使用的因子序列上，因子图卷积网络（FactorGraphGCN）**未优于** GRUAttention 与 TransformerSignal，且在方向命中率 / IC / ICIR 上一致落后。结论须如实表述为「**因子相关图聚合未带来改进，属无效 / 负向结果**」；本工作的真实价值在于诚实验证了「因子图结构偏置」对该数据集不成立，并框定了图神经网络在量化因子编码中的适用边界（需动态/可学习图或 richer 节点特征才可能有增益），而非宣称其超越时序基线。

## 可复现性

- 模型定义：`P1-QuantFactor/src/models/gnn_signal.py`（`FactorGraphGCN` + `build_factor_graph`）
- 训练入口：`P1-QuantFactor/scripts/04_train_nn.py --model gnn`
- 对照脚本：`P1-QuantFactor/scripts/37_gnn_compare.py`
- 结果产物：`data/P1/processed/report_gnn_vs_gru.json`（三方方向命中率 + IC/ICIR）、`report_gnn_h10.json`（分年 IC）
- 上述代码已纳入版本控制（仓库 `hzzqq/P1-QuantFactor`，与 #1/#3 同批次提交）。
