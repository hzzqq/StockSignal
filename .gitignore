__pycache__/
*.pyc
*.pyo
venv/
.venv/
env/
.env
*.db
data/cache.db
data/portfolio.csv
data/events.csv
*.xlsx
*.xls
.ipynb_checkpoints/
.DS_Store
Thumbs.db
.vscode/
.idea/
*.log

# 运行时本地产物（不入库）
logs/
data/
test_*.html
.streamlit/secrets.toml
*.bak.*
.cache/
deploy_preview/

# WorkBuddy 工作记忆与本地测试产物（含私有记忆/截图，不入库）
.workbuddy/

# 本地 pytest 临时目录（sandbox 禁止 rm 时保留，不入库）
.pytest_tmp/

# Windows 误创建的空设备文件（Git Bash/MSYS 下 `> nul` 会建出普通文件 nul，
# 它是保留设备名，删除需 cmd /c "del \\.\E:\project\ks\StockSignal\nul"，非 cmd 环境删不掉）
/nul
# 同上：`> 中文名` 重定向把变量/异常文本当文件名落盘（如「底层网络超时」）
/底层网络超时

# 本地页面冒烟测试报告（分批产物，本机生成，不入库）
tests/.pages_smoke_report*.json

# 覆盖率产物（观测模式本地产，不入库）
.coverage
.coverage.*
.coverage.json
htmlcov/

